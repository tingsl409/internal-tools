apiVersion: v1
kind: Service
metadata:
  name: {{ include "internal-tools.fullname" . }}
  labels:
    {{- include "internal-tools.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "internal-tools.selectorLabels" . | nindent 4 }}
